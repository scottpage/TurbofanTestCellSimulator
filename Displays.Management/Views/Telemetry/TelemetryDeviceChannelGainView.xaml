<UserControl x:Class="TelemetryDeviceChannelGainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="78" d:DesignWidth="279"
    xmlns:my="clr-namespace:ScottPage.RollsRoyce.TestCellSimulator.ManagementDisplay">
    <StackPanel>
        <TextBlock
            Margin="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=my:TelemetryDeviceChannelGainView, AncestorLevel=1}, Path=Header}"
            FontWeight="Bold" />
        <TextBlock
            Background="Black"
            Foreground="Lime"
            Margin="2"
            MinWidth="75"
            Padding="5"
            FontSize="20"
            TextAlignment="Center"
            Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=my:TelemetryDeviceChannelGainView, AncestorLevel=1}, Path=Gain}" />
    </StackPanel>
</UserControl>
